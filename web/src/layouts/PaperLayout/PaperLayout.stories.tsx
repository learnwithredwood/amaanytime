import React from 'react'

import { PaperLayout } from './PaperLayout'

export default {
  component: PaperLayout,
  title: 'PaperLayout',
}

const Template = (args) => <PaperLayout {...args} />

export const Default = Template.bind({})
